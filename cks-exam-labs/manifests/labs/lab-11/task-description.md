A deployment named `web-server` is running in the `restricted` namespace.

Identify the reason why the deployment is not in a running state. Fix the issue so that the deployment can be in a running state.

Requirements:

- Do not change the `restricted` namespace labels.
- Do not change the container image.

You can inspect the deployment, ReplicaSet, pods, namespace labels, and events with `kubectl`.

Validate your work with:

```bash
sh ~/val.sh
```
