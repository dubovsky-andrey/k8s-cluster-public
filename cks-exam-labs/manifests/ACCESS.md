# Access

## lab-01

```powershell
ssh student@<HOST_IP> -p 32001
```

Inside the SSH session:

```bash
sh ~/val.sh
```

Expected files:

```text
/home/student/.kube/config
/home/student/task.txt
/home/student/val.sh
```

The kubeconfig points only to the `lab-01` vCluster.

## lab-02

```powershell
ssh student@<HOST_IP> -p 32002
```

Inside the SSH session:

```bash
sh ~/val.sh
```

## lab-03

```powershell
ssh student@<HOST_IP> -p 32003
```

Inside the SSH session:

```bash
sh ~/val.sh
```

## lab-04

```powershell
ssh student@<HOST_IP> -p 32004
```

Inside the SSH session:

```bash
sh ~/val.sh
```

## lab-05

```powershell
ssh student@<HOST_IP> -p 32005
```

Inside the SSH session:

```bash
sh ~/val.sh
```

## lab-06

```powershell
ssh student@<HOST_IP> -p 32006
```

Inside the SSH session:

```bash
sh ~/val.sh
```

## lab-07

```powershell
ssh student@<HOST_IP> -p 32007
```

Inside the SSH session:

```bash
sh ~/val.sh
```

## lab-08

```powershell
ssh student@<HOST_IP> -p 32008
```

Inside the SSH session:

```bash
sh ~/val.sh
```
