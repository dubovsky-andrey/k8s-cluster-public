apiVersion: v1
kind: Service
metadata:
  name: argocd-server
  namespace: argocd
spec:
  selector:
    app.kubernetes.io/name: argocd-server
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 8080
