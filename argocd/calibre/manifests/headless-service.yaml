apiVersion: v1
kind: Service
metadata:
  name: calibre-headless
  namespace: calibre
  labels:
    app.kubernetes.io/name: calibre
    app.kubernetes.io/managed-by: argocd
spec:
  clusterIP: None
  selector:
    app: calibre-web
  ports:
    - name: http
      port: 8083
      targetPort: http
