apiVersion: v1
kind: Service
metadata:
  name: calibre-web
  namespace: calibre
  labels:
    app.kubernetes.io/name: calibre
    app.kubernetes.io/managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: calibre-web
  ports:
  - name: http
    port: 30003
    targetPort: 30003
