apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: moodle-route
  namespace: moodle
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main-gateway
      namespace: gatewayapi
  hostnames:
    - moodle.h.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: moodle
          port: 80
          weight: 1
